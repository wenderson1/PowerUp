{
  "name": "powerup",
  "version": "1.0.0",
  "description": "Offline-first Android workout tracking app",
  "private": true,
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "test": "vitest run",
    "cap:sync": "npx cap sync"
  },
  "dependencies": {
    "@capacitor-community/sqlite": "^7.0.0",
    "@capacitor/android": "^7.0.0",
    "@capacitor/core": "^7.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.3.1",
    "vite": "^6.0.0",
    "vitest": "^3.0.0"
  }
}
